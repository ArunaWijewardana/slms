{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'User Category'|trans }}</h1>

    <ul>
        <a href="{{ path('usercategory_index') }}" class="btn btn-secondary">{{ 'Back to the list'|trans }}</a>
        <a href="{{ path('usercategory_edit', { 'id': userCategory.id }) }}" class="btn btn-success">{{ 'Edit'|trans }}</a>
    </ul>

    <table class="table table-striped table-dark">
        <tbody>
            <tr>
                <th class="text-right">{{ 'Id'|trans }}</th>
                <td>{{ userCategory.id }}</td>
            </tr>
            <tr>
                <th class="text-right">{{ 'Title'|trans }}</th>
                <td>{{ userCategory.title }}</td>
            </tr>
            <tr>
                <th class="text-right">{{ 'Description'|trans }}</th>
                <td>{{ userCategory.description }}</td>
            </tr>
            <tr>
                <th class="text-right">{{ 'Status'|trans }}</th>
                <td>{{ userCategory.status }}</td>
            </tr>
            <tr>
                <th class="text-right">{{ 'Created at'|trans }}</th>
                <td>{{ userCategory.createdAt|date }}</td>
            </tr>
            <tr>
                <th class="text-right">{{ 'Modified at'|trans }}</th>
                <td>{{ userCategory.modifiedAt|date }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        {{ form_start(delete_form) }}
            <input type="submit" value="{{ 'Delete'|trans }}" class="btn btn-danger">
        {{ form_end(delete_form) }}
    </ul>
{% endblock %}
